package cyclops.reactive.collection.function.higherkinded;

public interface ReactiveWitness {

    enum sortedSet {}

    enum set {}

    enum list {}

    enum linkedListX {}

    enum vectorX {}

    enum persistentQueueX {}

    enum persistentSetX {}

    enum orderedSetX {}

    enum bagX {}

    enum deque {}

    enum queue {}
}
